language: python
os: linux
dist: bionic

python:
  - "3.5"
  - "3.6"
  - "3.7"

install:
  - pip install --upgrade pip
  - pip install -r requirements.txt
  - pip install -r tests/requirements.txt
  - pip install .

script:
  - cd tests
  - pytest
