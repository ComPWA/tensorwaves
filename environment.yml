name: tensorwaves
channels:
  - defaults
dependencies:
  - python==3.8.*
  - pip>=19
  - graphviz # for binder
  - pip:
      - |
        -c .constraints/py3.8.txt
        -e .[dev]
variables:
  PRETTIER_LEGACY_CLI: "1"
