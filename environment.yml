name: tensorwaves
channels:
  - defaults
dependencies:
  - python==3.10.*
  - pip>=19
  - graphviz # for binder
  - pip:
      - -c .constraints/py3.10.txt -e .[dev]
variables:
  PYDEVD_DISABLE_FILE_VALIDATION: 1
  PYTHONHASHSEED: 0
