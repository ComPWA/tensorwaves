name: tensorwaves
channels:
  - defaults
dependencies:
  - python==3.9.*
  - pip>=19
  - graphviz # for binder
  - pip:
      - -c .constraints/py3.9.txt -e .[dev]
variables:
  PRETTIER_LEGACY_CLI: "1"
  PYTHONHASHSEED: 0
